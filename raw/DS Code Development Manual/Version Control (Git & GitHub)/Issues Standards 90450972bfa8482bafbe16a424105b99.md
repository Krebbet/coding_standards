# Issues Standards

When a pull request or commit references a keyword and issue number, it creates an association between the pull request and the issue. When the pull request is merged into your repository's default branch, the corresponding issue is automatically closed.

The following keywords, followed by an issue number, will close the issue:

```
close
closes
closed
fix
fixes
fixed
resolve
resolves
resolved
```

For example, to close an issue numbered 123, you could use the phrase `Closes #123` or `Closes: #123` in your pull request description or commit message. Once the branch is merged into the default branch, the issue will close.

# How to Create Issues

To open a new issue, navigate to your project repository on the SFL GitHub page, click on Issues and then New Issue.  Fill in the note with **detailed** info about the bug and give example(s) of the problem. Click Submit new issue when you're happy with your **detailed** note. 

# How to Close Issues with Commits

When a pull request or commit references a keyword and issue number, it creates an association between the pull request and the issue. When the pull request is merged into your repository's mater branch, the corresponding issue is automatically closed.

For example, to close an issue numbered 123, you could use the phrase "Closes #123" or "Closes: #123" in your pull request description or commit message. Once the branch is merged into the default branch, the issue will close.