# Log Not Print

Level: Class, Function, Library
Constraint: Hard
Last Edit: June 17, 2024 3:08 PM
Project Types: GA, MVP, POC

## Log Not Print

In scripts (i.e. not in notebooks) the default behavior should always be to log messages and not print; this allows tracking of issues for downstream analysis and is in general good accounting. 

Be careful to ensure that logging configurations are only set within entrypoints (e.g. inside some if **name** == “__main__”. If it is set in the more general scope then importing from that file can change the config and cause confusion/issues.

**Bad:**

```python
# BAD
print("Hello World...")
print("Model training has completed in t="+str(x))
```

**Good:**

```python
import logging
logger = logging.getLogger(__name__)

...
logger.info("Training top model layers ...")

if __name__ == "__main__":
	logging.basicConfig(level=logging.INFO)
```